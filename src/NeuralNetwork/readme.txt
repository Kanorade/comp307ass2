To run a2part1.jar, ensure the training and test csv is in the same directory and run the command

    java -jar a2Part1.jar

The executable assumes the first line are labels. It also assumes the last value of the remaining
lines to be the classifier string and the rest of the values are real numbers.